# Level Forge Assets

Use these folders for new uploads going forward:

- `assets/skills` for skill icons.
- `assets/pets` for pet images.
- `assets/backgrounds` for cosmetic backgrounds.
- `assets/township` for building and township images.

Older image folders are still used by the current site. Do not move existing image files unless the code references are updated in the same change.
